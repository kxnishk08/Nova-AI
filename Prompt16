The current failure is confirmed as an Azure AI Search API-version mismatch.

Current error:

The property 'vectorSearchProfile' does not exist on type
Microsoft.Azure.Search.V2023_07_01_Preview.SchemaField
or is not present in API version '2023-07-01-Preview'.

We should NOT remove vectorSearchProfile. The modern vector index schema requires it.

Microsoft's current Azure AI Search REST API supports vector profiles and vectorSearchProfile in newer stable API versions such as 2024-07-01.

Make the MINIMAL change needed:

1. In:
   src/NovaBanking.Infrastructure/Search/AzureAiSearchIndexInitializer.cs

   Find the Azure AI Search index REST API URL currently using:
   api-version=2023-07-01-Preview

   Change ONLY the index-management API version to:
   api-version=2024-07-01

2. Keep the current vector schema exactly as follows:

   vectorSearch:
     algorithms:
       name: vectorConfig
       kind: hnsw
       hnswParameters:
         m: 4
         efConstruction: 400
         efSearch: 500
         metric: cosine

     profiles:
       name: vector-profile
       algorithm: vectorConfig

3. Keep the vector field configured with:

   vectorSearchProfile: vector-profile

4. Do NOT revert to the old "parameters" property.

5. Do NOT change:
   - Azure OpenAI endpoint
   - embedding deployment
   - embedding API key
   - Search endpoint/key
   - SQL/database
   - authentication
   - MCP
   - PDF extraction
   - chunking
   - RAG architecture

6. IMPORTANT: inspect AzureAiSearchKnowledgeService.cs as well.
   If its Search REST request uses an old Azure AI Search API version or old vector-query JSON syntax that is incompatible with 2024-07-01, update ONLY that API version/query syntax as required by the 2024-07-01 REST API.

   Do not redesign the search implementation.

7. Run:
   dotnet build
   dotnet test
   dotnet build tools/KnowledgeIngestor
   dotnet run --project tools\KnowledgeIngestor

8. If index creation succeeds but document upload or vector querying fails, stop and report the exact error. Do not make speculative changes.

The goal is:
PDF extraction → chunking → embeddings → create vector index → upload documents/vectors.

Do not modify unrelated code.
