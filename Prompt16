Now verify the RAG retrieval path end-to-end.

Do NOT change the existing architecture or ingestion pipeline.

We have confirmed:
- Azure AI Search index: nova-banking-knowledge-index
- 4 documents
- 12 chunks uploaded
- embeddings generated successfully
- 26 tests passing

I want a minimal read-only verification of the existing RAG search implementation.

Tasks:

1. Inspect the existing AzureAiSearchKnowledgeService implementation.

2. Verify that vector search:
   - generates an embedding for the user's question using the same text-embedding-3-small deployment
   - queries nova-banking-knowledge-index
   - uses the correct vector field
   - uses the correct vectorSearchProfile
   - returns the top relevant chunks
   - preserves document metadata such as DocumentId, Title, Category, Version, EffectiveDate, PageNumber, SourceUrl

3. Run a simple test query against the existing index, for example:

   "What are the rules for savings account minimum balance?"

4. Report:
   - whether the query embedding succeeds
   - whether Azure AI Search returns results
   - number of results
   - titles/categories of returned documents
   - page numbers
   - short sanitized excerpts sufficient to prove relevance

5. Do NOT print API keys, connection strings, customer data, session IDs, or other secrets.

6. If the current search implementation is incompatible with the 2024-07-01 Search API, make only the minimum required correction.

7. Run:
   dotnet build
   dotnet test

8. Do NOT modify authentication, MCP, SQL, banking services, ingestion, PDF extraction, or unrelated code.

If retrieval succeeds, stop and report the evidence.

If retrieval fails, stop at the first error and report the exact HTTP status, sanitized response body, URL/API operation, and relevant code location. Do not make speculative changes.
