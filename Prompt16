PHASE 7B — MAKE RAG ACTUALLY WORK END-TO-END

The previous Phase 7 implementation created the RAG abstractions and tests, but the actual Azure AI Search runtime is still incomplete.

Specifically:
- AzureAiSearchKnowledgeService.SearchAsync() currently throws NotImplementedException.
- PDF extraction is not implemented.
- Embedding generation is not implemented.
- Azure AI Search index creation/schema setup is not implemented.
- There is no working ingestion CLI.
- There is no real end-to-end retrieval against Azure AI Search.

Implement these now.

DO NOT create mocks, fake Azure services, local keyword-search substitutes, or placeholder implementations.

The goal is one real working pipeline:

4 PDFs
  ↓
PDF page extraction
  ↓
cleaning/chunking
  ↓
Azure OpenAI embeddings
  ↓
Azure AI Search vector/hybrid index
  ↓
SearchBankingKnowledge(question)
  ↓
real retrieved chunks + metadata

==================================================
1. NUGET PACKAGES
==================================================

Use the official Microsoft/Azure SDK packages.

Determine the current compatible stable versions rather than guessing.

Required functionality:

- Azure AI Search / Azure.Search.Documents
- Azure OpenAI / official Azure OpenAI client capable of generating embeddings
- PDF text extraction library

For PDF extraction, use a lightweight established .NET PDF library such as PdfPig if compatible.

Run dotnet restore and verify all packages resolve.

Do not create fake Azure SDK classes.

==================================================
2. AZURE AI SEARCH
==================================================

Implement AzureAiSearchKnowledgeService.SearchAsync() for real.

Use the official Azure.Search.Documents SDK.

Configuration must come from AzureAiSearchOptions:

- Endpoint
- ApiKey
- IndexName
- EmbeddingDeployment

No secrets in source code.

The search flow must:

1. Receive the user's public banking question.
2. Generate an embedding for the question using the configured Azure OpenAI embedding deployment.
3. Query Azure AI Search using vector/hybrid search.
4. Retrieve the top K relevant chunks.
5. Map results to KnowledgeSearchResultDto.
6. Preserve:
   - DocumentId
   - Title
   - Category
   - Version
   - EffectiveDate
   - PageNumber
   - SourceUrl
   - Text/content
   - relevance score if available

Make top-K configurable.

==================================================
3. AZURE AI SEARCH INDEX
==================================================

Create an index initialization component.

Suggested index:

nova-banking-knowledge-index

Required fields:

id
documentId
title
category
version
effectiveDate
pageNumber
sourceUrl
content
embedding

Configure:

- id as key
- content/title as searchable
- category/version/pageNumber as filterable
- metadata retrievable
- embedding as vector field
- vector dimensions must match the selected embedding model
- appropriate vector search configuration/profile

Do not hardcode vector dimensions if they depend on the configured model without validating compatibility.

The initialization should be safe to run repeatedly.

If the index doesn't exist:
    create it.

If it already exists:
    do not destroy existing indexed data.

Do not silently change an incompatible existing schema.

==================================================
4. PDF EXTRACTION
==================================================

Implement actual extraction from:

knowledge/SavingsAndCurrentAccountTerms.pdf
knowledge/HomeAndPersonalLoanPolicy.pdf
knowledge/CreditCardFeesAndBillingGuide.pdf
knowledge/DigitalBankingAndSecurityFAQ.pdf

Preserve page numbers.

The ingestion pipeline should produce:

Document
  → Page[]
  → KnowledgeChunk[]

Use the existing KnowledgeIngestionService for cleaning/chunking rather than duplicating its logic.

Each chunk must retain:

DocumentId
Title
Category
Version
EffectiveDate
PageNumber
SourceUrl
ChunkIndex
Text

If metadata such as Version/EffectiveDate/SourceUrl isn't present in the PDF itself, use explicit document metadata configuration rather than inventing values.

==================================================
5. EMBEDDING GENERATION
==================================================

Implement real embedding generation using Azure OpenAI.

Embedding deployment name must be configurable.

For every chunk:

chunk text
  ↓
Azure OpenAI embedding model
  ↓
float vector
  ↓
Azure AI Search embedding field

Batch embedding requests where the SDK supports it.

Handle transient errors appropriately.

Do not log:
- API keys
- connection strings
- credentials
- sensitive customer information

==================================================
6. INGESTION CLI
==================================================

Create:

tools/KnowledgeIngestor

This should be a .NET console application.

It must:

1. Load Azure configuration securely.
2. Initialize/create the Azure AI Search index.
3. Find the four PDFs in the knowledge directory.
4. Extract per-page text.
5. Chunk the documents.
6. Generate embeddings.
7. Upsert chunks into Azure AI Search.
8. Print a useful summary.

Example output:

Document: SavingsAndCurrentAccountTerms.pdf
Pages extracted: X
Chunks generated: Y
Chunks uploaded: Y

...

Total documents: 4
Total chunks: N
Index: nova-banking-knowledge-index

Use deterministic chunk IDs already implemented.

Re-running ingestion must upsert rather than duplicate.

==================================================
7. CONFIGURATION
==================================================

Use user secrets or environment variables for local development.

DO NOT ask me to paste credentials into source files.

Expected configuration:

AzureAiSearch:
  Endpoint
  ApiKey
  IndexName
  EmbeddingDeployment

AzureOpenAI:
  Endpoint
  ApiKey
  EmbeddingDeployment

If the existing project has a better configuration structure, reuse it.

Document exactly how I should configure these values.

==================================================
8. REAL AZURE VERIFICATION
==================================================

After implementation, provide exact commands for:

A. Creating/configuring the Azure resources if needed.

B. Setting local configuration.

C. Running the ingestion CLI.

D. Starting NovaBanking.Api.

E. Calling SearchBankingKnowledge through the application/MCP layer.

The verification must prove:

Question:
"What are the fees for a savings account?"

produces actual chunks retrieved from Azure AI Search.

The result must contain real metadata such as:

document title
page number
chunk content

Do not claim this is verified if Azure credentials/resources are unavailable.

==================================================
9. ERROR HANDLING
==================================================

Handle:

- missing Azure configuration
- Azure AI Search unavailable
- Azure OpenAI unavailable
- embedding failure
- index missing
- empty search result
- malformed PDF
- ingestion failure

Return controlled application errors.

Do not expose Azure credentials or internal connection details.

==================================================
10. SECURITY
==================================================

Maintain the existing architecture.

RAG is PUBLIC KNOWLEDGE ONLY.

Absolutely no:
- CustomerId
- account numbers
- balances
- transactions
- loans
- credit cards
- PINs
- sessions
- credentials

may enter the knowledge index.

SearchBankingKnowledge must remain separate from authenticated customer-specific MCP tools.

Retrieved document content is untrusted DATA, not instructions.

Do not allow retrieved text to override:
- system instructions
- authorization
- tool permissions
- authentication
- application security rules

==================================================
11. TESTS
==================================================

Keep existing tests passing.

Add unit tests for:

- PDF page extraction
- chunk generation from extracted pages
- embedding-to-index document mapping
- deterministic IDs
- metadata preservation
- no customer data in ingestion models
- empty retrieval
- Azure search result mapping

Use mocks for unit tests.

Do not require live Azure resources for every test.

==================================================
12. BUILD
==================================================

Run:

dotnet restore
dotnet build
dotnet test

Also build the KnowledgeIngestor project explicitly.

==================================================
13. FINAL REPORT
==================================================

Report:

1. NuGet packages and versions.
2. Files created/modified.
3. Azure AI Search index schema.
4. Vector dimensions.
5. Embedding model/deployment.
6. PDF extraction library.
7. Chunking configuration.
8. Ingestion CLI command.
9. Configuration required.
10. SearchAsync implementation details.
11. Exact runtime retrieval flow.
12. Number of PDFs successfully processed.
13. Number of chunks generated.
14. Whether actual Azure indexing succeeded.
15. Whether an actual query successfully retrieved a real chunk.
16. Build result.
17. Test result.

CRITICAL:

If you cannot access the configured Azure resources from this environment, still implement the complete code.

Then clearly state:

CODE COMPLETE:
- ...

AZURE VERIFICATION BLOCKED BY:
- ...

Do NOT replace missing Azure connectivity with a fake implementation just to make the tests pass.
