The Azure deployment issue is identified.

The text-embedding-3-small deployment is NOT on the novaai2026.openai.azure.com resource.

The deployment details page shows:

Deployment name:
text-embedding-3-small

Status:
Succeeded

Endpoint:
https://kanishksharma-7060-resource.services.ai.azure.com

Therefore update ONLY the embedding configuration so that the RAG embedding calls use:

AzureAiSearch:OpenAiEndpoint =
https://kanishksharma-7060-resource.services.ai.azure.com

AzureAiSearch:EmbeddingDeployment =
text-embedding-3-small

Do NOT change the GPT-4o configuration, Azure OpenAI configuration, Azure AI Search configuration, database configuration, or code architecture.

The embedding implementation should continue using:

POST {endpoint}/openai/v1/embeddings

with:

{
  "input": [...],
  "model": "text-embedding-3-small"
}

using the api-key header.

Do not expose or print any secrets.

After updating the configuration:
1. Build the solution.
2. Run all tests.
3. Build KnowledgeIngestor.
4. Run:
   dotnet run --project tools\KnowledgeIngestor

Report the exact result, especially whether index creation and PDF ingestion now proceed past embedding generation.

Do not make speculative code changes if another error occurs. Stop and report the exact error.
