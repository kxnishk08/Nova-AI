We have identified the current RAG retrieval failure.

Azure OpenAI query embedding succeeds.
Azure AI Search index exists and contains 12 chunks.
The current search request returns:

HTTP 405 MethodNotAllowed

Current incorrect request:
POST
/indexes/nova-banking-knowledge-index/search?api-version=2024-07-01

Current incorrect body shape:
{
  "vector": {
    "value": [...],
    "fields": ["embedding"],
    "k": 5,
    "profile": "vector-profile"
  },
  "select": [...],
  "query": "*"
}

According to the Azure AI Search REST API, the correct search operation is:

POST
/indexes/nova-banking-knowledge-index/docs/search?api-version=2024-07-01

The vector query should use the vectorQueries array.

Make the MINIMAL change in:

src/NovaBanking.Infrastructure/Search/AzureAiSearchKnowledgeService.cs

Use this request structure:

{
  "count": true,
  "select": "documentId,title,category,version,effectiveDate,pageNumber,sourceUrl,content",
  "top": 5,
  "vectorQueries": [
    {
      "kind": "vector",
      "vector": [ ...embedding floats... ],
      "fields": "embedding",
      "k": 5
    }
  ]
}

Important:
- fields must be the string "embedding", not an array.
- vector must be the embedding float array directly.
- use vectorQueries, not vector.
- do NOT put "profile" inside the vector query unless the 2024-07-01 API specifically requires it.
- Do NOT use "query": "*" unless needed for hybrid search.
- Keep the API version 2024-07-01.
- Keep the existing Azure Search endpoint and API key configuration.
- Keep the existing text-embedding-3-small embedding flow.
- Keep the existing index and vectorSearchProfile configuration.
- Do NOT recreate or redesign the index.
- Do NOT change Azure resources.
- Do NOT change PdfPig.
- Do NOT change MCP/authentication/SQL.

The endpoint should therefore be constructed as:

_options.Endpoint
    + "/indexes/"
    + _options.IndexName
    + "/docs/search?api-version=2024-07-01"

After the minimal change:

1. dotnet build
2. dotnet test
3. Run the existing retrieval verification.
4. Use the test question:
   "What are the rules for savings account minimum balance?"

Report:
- whether query embedding succeeds
- HTTP status from Azure AI Search
- number of returned results
- returned document titles/categories/page numbers
- short sanitized content excerpts proving the result is relevant

Do not print API keys, connection strings, customer data, session IDs, or secrets.

If the request gets a 400 instead of 200, stop and report the exact sanitized response. Do not make further speculative changes.
