Proceed with diagnosing and fixing the current KnowledgeIngestor embedding failure.

Current status:
- PdfPig dependency mismatch: FIXED
- PDF extraction: WORKING
- Chunking: WORKING
- Azure OpenAI embedding configuration was previously fixed
- Azure AI Search index API version: 2024-07-01
- Azure AI Search vector schema: WORKING
- Solution build: PASSED
- Tests: 26 PASSED

KnowledgeIngestor now reports:

Pages extracted: 3
Chunks generated: 3

Then:

System.InvalidOperationException:
Embedding request failed: NotFound

at tools/KnowledgeIngestor/Program.cs line 70

Important context:
There is already a shared implementation:

src/NovaBanking.Infrastructure/Search/OpenAiRestEmbeddings.cs

It uses the Azure OpenAI-compatible v1 embeddings endpoint:

POST {endpoint}/openai/v1/embeddings

with:

{
  "input": [...],
  "model": "text-embedding-3-small"
}

The KnowledgeIngestor previously had a separate local embedding implementation.

Tasks:

1. Inspect the CURRENT tools/KnowledgeIngestor/Program.cs embedding implementation.
2. Determine the exact URL it is calling, without printing API keys/secrets.
3. Determine the effective OpenAiEndpoint and EmbeddingDeployment configuration source/value (endpoint and deployment only; never print the key).
4. Compare its implementation with:
   src/NovaBanking.Infrastructure/Search/OpenAiRestEmbeddings.cs

5. If KnowledgeIngestor is still using its own outdated embedding implementation, replace that local implementation with the existing shared OpenAiRestEmbeddings.GetEmbeddingsAsync helper.

6. The KnowledgeIngestor should use:
   endpoint:
   https://kanishksharma-7060-resource.services.ai.azure.com

   deployment/model:
   text-embedding-3-small

   endpoint:
   {endpoint}/openai/v1/embeddings

7. Preserve the existing shared UserSecrets configuration. Do not hardcode secrets.

8. Do NOT change:
   - Azure resources
   - deployment
   - API keys
   - Azure AI Search
   - vector schema
   - PdfPig dependencies
   - PDF extraction/chunking
   - MCP
   - authentication
   - database
   - unrelated code

9. After the minimal fix run:
   dotnet build
   dotnet test
   dotnet build tools/KnowledgeIngestor
   dotnet run --project tools\KnowledgeIngestor

10. If the embedding request still returns NotFound:
   STOP.
   Report:
   - exact URL
   - deployment/model name
   - HTTP status
   - sanitized response body
   - effective endpoint source
   - effective deployment source

   Do not make another speculative Azure/code change.

Do not print or expose any API key.
