Proceed with OPTION B.

Adapt the implementation to the EXACT Azure SDK versions currently restored in this project. Do NOT downgrade packages merely to match previously written code.

PHASE 7C — FIX AZURE SDK COMPATIBILITY AND FINISH REAL RAG

Goal: get the entire RAG pipeline compiling and working with the actual package versions restored by dotnet restore.

CURRENT STATE

The following components already exist:

- IKnowledgeSearchService
- KnowledgeSearchResultDto
- KnowledgeChunk
- KnowledgeIngestionService
- AzureAiSearchOptions
- AzureAiSearchKnowledgeService
- AzureAiSearchIndexInitializer
- tools/KnowledgeIngestor
- RagTests
- MCP SearchBankingKnowledge integration

The implementation currently fails to build because the Azure SDK APIs used by the code don't match the versions actually restored.

DO NOT create shims.
DO NOT fake Azure APIs.
DO NOT replace Azure AI Search with local keyword search.
DO NOT downgrade packages simply to match old examples.

==================================================
1. FIRST: INSPECT ACTUAL RESOLVED VERSIONS
==================================================

Run:

dotnet list package --include-transitive

for the relevant projects.

Record the exact versions of:

- Azure.Search.Documents
- Azure.AI.OpenAI, if still used
- Azure.Core
- PdfPig / UglyToad.PdfPig
- any other Azure packages involved in RAG

Use the APIs belonging to those EXACT versions.

Do not assume API signatures from older tutorials or documentation.

==================================================
2. AZURE AI SEARCH
==================================================

Finish AzureAiSearchKnowledgeService.SearchAsync() using the actual Azure.Search.Documents API that is installed.

The implementation must perform REAL Azure AI Search retrieval.

Required flow:

question
  ↓
embedding
  ↓
Azure AI Search vector/hybrid search
  ↓
top K results
  ↓
KnowledgeSearchResultDto

Support the required metadata:

- DocumentId
- Title
- Category
- Version
- EffectiveDate
- PageNumber
- SourceUrl
- Text
- score when available

If the installed SDK exposes vector search through different types than the code currently uses, rewrite the implementation to those actual types.

Do not work around compilation errors with dynamic/reflection unless absolutely required by the SDK.

==================================================
3. EMBEDDINGS
==================================================

Use a supported official Azure OpenAI approach compatible with the packages actually installed.

First inspect whether the current Azure.AI.OpenAI package supports the embedding API required.

If yes:
- use it.

If the installed Azure.AI.OpenAI API is awkward/incompatible:
- use the official Azure OpenAI REST API for embeddings,
- but do NOT fake the API.

Embedding configuration must remain externalized.

Do not hardcode:
- endpoint
- API key
- deployment name

The embedding deployment name must be configurable.

==================================================
4. INDEX INITIALIZER
==================================================

Finish AzureAiSearchIndexInitializer using the actual installed Azure.Search.Documents API.

It must create:

nova-banking-knowledge-index

with fields:

id
documentId
title
category
version
effectiveDate
pageNumber
sourceUrl
content
embedding

Configure the vector field correctly for the installed SDK/API.

The embedding dimensions must match the actual embedding model configured.

Make index creation idempotent.

If the index already exists:
- don't delete it
- don't destroy data
- don't silently replace an incompatible schema

==================================================
5. PDF EXTRACTION
==================================================

Finish the PDF extraction implementation using the ACTUAL resolved PdfPig package version.

The four files are:

knowledge/SavingsAndCurrentAccountTerms.pdf
knowledge/HomeAndPersonalLoanPolicy.pdf
knowledge/CreditCardFeesAndBillingGuide.pdf
knowledge/DigitalBankingAndSecurityFAQ.pdf

Extract text page-by-page and preserve page numbers.

Then pass those pages through the existing KnowledgeIngestionService.

Do not duplicate chunking logic.

==================================================
6. KNOWLEDGE INGESTOR
==================================================

Finish tools/KnowledgeIngestor as a real executable console application.

It must:

1. Read the four PDFs.
2. Extract pages.
3. Clean/chunk them.
4. Generate embeddings.
5. Upsert documents into Azure AI Search.
6. Print counts/results.

Use deterministic IDs already implemented.

Re-running ingestion must update/upsert existing chunks rather than create duplicates.

Example command:

dotnet run --project tools/KnowledgeIngestor

Add sensible command-line options if useful, but don't over-engineer this.

==================================================
7. CONFIGURATION
==================================================

Use the existing configuration classes.

Document exact configuration keys.

At minimum support:

AzureAiSearch:Endpoint
AzureAiSearch:ApiKey
AzureAiSearch:IndexName
AzureAiSearch:EmbeddingDeployment

and whatever Azure OpenAI endpoint/key configuration is actually required by the chosen implementation.

Secrets must NOT be committed to source code.

If user secrets are appropriate, document:

dotnet user-secrets ...

Do not ask me to paste secrets into source code.

==================================================
8. RAG SEARCH SEMANTICS
==================================================

Prefer hybrid/vector retrieval if the installed SDK supports it cleanly.

The implementation must genuinely use the embedding vector for semantic retrieval.

Return a bounded top-K result set.

If no sufficiently relevant result exists:
- return an empty/controlled result
- do not hallucinate banking policy information.

==================================================
9. MCP INTEGRATION
==================================================

Preserve the existing real MCP implementation.

SearchBankingKnowledge(question)
    ↓
IBankingService.SearchBankingKnowledgeAsync()
    ↓
IKnowledgeSearchService.SearchAsync()
    ↓
AzureAiSearchKnowledgeService
    ↓
Azure AI Search

Do not let MCP directly query Azure.

Do not modify the authenticated customer-specific MCP architecture.

==================================================
10. SECURITY
==================================================

Maintain all existing security guarantees.

RAG contains ONLY public banking knowledge.

Never ingest:

- CustomerId
- account numbers
- balances
- transactions
- loans belonging to customers
- credit card customer data
- PINs
- credentials
- sessions
- authentication tokens

Retrieved text is untrusted reference DATA.

It cannot override system/application instructions or security rules.

==================================================
11. TESTS
==================================================

Fix/update existing tests to match the final implementation.

Run:

dotnet restore
dotnet build
dotnet test

Also explicitly build:

dotnet build tools/KnowledgeIngestor

Unit tests must not require live Azure credentials.

Use mocks/fakes around external Azure calls where appropriate.

==================================================
12. IMPORTANT: DO NOT STOP AT "CODE SCAFFOLDING"
==================================================

The previous phase already created the scaffolding.

This phase is specifically to finish the implementation.

Do not report:

"SearchAsync will be implemented later."

Do not leave:

NotImplementedException

in the production RAG path.

Do not leave placeholder vector-search code.

Do not leave placeholder embedding code.

Do not leave a pseudo ingestion command when the CLI is supposed to exist.

==================================================
13. AZURE VERIFICATION
==================================================

If valid Azure credentials/configuration are already available in the environment:

- create/verify the index
- run the KnowledgeIngestor
- ingest the four PDFs
- execute one real search
- report the actual number of indexed chunks
- report an example retrieved document/page

If Azure credentials are NOT available:

still finish all production code and unit tests.

Then clearly report:

CODE:
complete

LIVE AZURE VERIFICATION:
blocked because <exact missing configuration/resource>

Do NOT fake successful Azure verification.

==================================================
14. FINAL REPORT
==================================================

Return:

A. Exact resolved package versions.

B. Changes made to fix each SDK mismatch.

C. Final Azure AI Search implementation.

D. Final embedding implementation.

E. PDF extraction implementation.

F. Index schema and vector dimensions.

G. KnowledgeIngestor command.

H. Configuration keys.

I. Whether live Azure ingestion was successfully executed.

J. Number of PDFs processed.

K. Number of chunks generated/indexed.

L. Example real retrieval result if Azure verification succeeded.

M. Build result.

N. Test result.

O. Confirm whether any NotImplementedException remains in the RAG production path.

Most important:
KEEP WORKING UNTIL dotnet build, dotnet test, AND the KnowledgeIngestor project build successfully.
