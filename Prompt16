We have fixed the Azure OpenAI embedding configuration.

The embedding request now succeeds, and the KnowledgeIngestor has progressed to Azure AI Search index creation.

The ONLY current failure is:

Index creation failed:
BadRequest -
Cannot find nested property 'parameters' on the resource type
'Microsoft.Azure.Search.V2023_07_01_Preview.VectorSearchAlgorithmConfiguration'

Fix ONLY the Azure AI Search index schema in:

src/NovaBanking.Infrastructure/Search/AzureAiSearchIndexInitializer.cs

The current vectorSearch algorithm configuration is using:

"parameters": {
    "m": 4,
    "efConstruction": 400,
    "efSearch": 500,
    "metric": "cosine"
}

Change it to the Azure AI Search REST schema:

"vectorSearch": {
  "algorithms": [
    {
      "name": "vectorConfig",
      "kind": "hnsw",
      "hnswParameters": {
        "m": 4,
        "efConstruction": 400,
        "efSearch": 500,
        "metric": "cosine"
      }
    }
  ],
  "profiles": [
    {
      "name": "vector-profile",
      "algorithm": "vectorConfig"
    }
  ]
}

Make sure the embedding vector field references:

"vectorSearchProfile": "vector-profile"

Do not change:
- Azure OpenAI endpoint
- embedding API key
- embedding deployment
- GPT configuration
- SQL/database
- authentication
- MCP
- RAG architecture
- chunking
- PDF extraction

Use the existing Azure AI Search API version unless the current implementation absolutely requires another version.

After the minimal change:
1. dotnet build
2. dotnet test
3. dotnet build tools/KnowledgeIngestor
4. dotnet run --project tools\KnowledgeIngestor

Do not make speculative changes.

Report the result. If another Azure AI Search schema error occurs, stop and report the exact error rather than changing unrelated code.
