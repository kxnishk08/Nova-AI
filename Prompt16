The UserSecrets configuration issue is fixed.

KnowledgeIngestor now successfully loads the shared secrets, reaches Azure OpenAI, and receives:

HTTP 404
{"error":{"code":"404","message":"Resource not found"}}

The current implementation uses the older Azure OpenAI embeddings route:

/openai/deployments/{deployment}/embeddings?api-version=2023-10-01

Update the embedding implementation to the current Azure OpenAI OpenAI-compatible v1 embeddings API.

IMPORTANT:
- Do not change the Azure resource.
- Do not create another deployment.
- Do not change the deployment name.
- Do not change the User Secrets.
- Do not add Azure.AI.OpenAI.
- Do not modify MCP/authentication/database code.

CURRENT AZURE CONFIGURATION

Azure OpenAI resource:
novaai2026

Azure OpenAI endpoint:
https://novaai2026.openai.azure.com/

Embedding deployment:
text-embedding-3-small

The deployment is confirmed as Succeeded in Microsoft Foundry.

TASK

1. Update the REST embedding implementation in:

src/NovaBanking.Infrastructure/Search/AzureAiSearchKnowledgeService.cs

and

tools/KnowledgeIngestor/Program.cs

to use:

POST {endpoint}/openai/v1/embeddings

2. Use the OpenAI-compatible request format:

{
  "input": ["text 1", "text 2"]
  "model": "text-embedding-3-small"
}

Use the configured deployment name for the model value. Do not hardcode it.

3. Authenticate using:

api-key: <configured Azure OpenAI API key>

4. Parse:

data[].embedding

into the existing float[] representation.

5. Do not use:

/openai/deployments/{deployment}/embeddings?api-version=2023-10-01

anymore.

6. Keep the existing configuration keys:

AzureAiSearch:OpenAiEndpoint
AzureAiSearch:OpenAiApiKey
AzureAiSearch:EmbeddingDeployment

7. Make sure endpoint normalization doesn't produce a malformed URL if the configured endpoint ends with '/'.

8. Use the same embedding implementation for:
   - index dimension detection
   - document/chunk embeddings during ingestion
   - runtime query embeddings

Avoid having three different embedding implementations.

9. Do not change Azure AI Search code yet.

10. Run:

dotnet restore
dotnet build
dotnet test
dotnet build tools/KnowledgeIngestor

11. Then run:

dotnet run --project tools/KnowledgeIngestor

EXPECTED RESULT

The program should get past:

"Unable to obtain embedding dimension"

and proceed to:
- index initialization
- PDF extraction
- chunking
- embedding generation
- Azure AI Search upsert

If the new v1 embeddings request still returns 404, STOP.

Do not make additional speculative code changes.

Instead report:
- exact HTTP URL used, WITHOUT the API key
- deployment/model name used
- HTTP status
- response body
- whether the request reached novaai2026

Do not print any API keys or secret values.

Also report whether the Azure OpenAI v1 endpoint is supported by the resource.

The goal is to determine whether the problem is the old API route or whether the deployment is associated with a different Foundry/Azure resource.
