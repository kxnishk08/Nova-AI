The KnowledgeIngestor builds successfully now, but when I run:

dotnet run --project tools\KnowledgeIngestor

it reports:

"AzureAiSearch and AzureOpenAI configuration missing."

The reason is that I configured the Azure secrets using User Secrets for NovaBanking.Api, but KnowledgeIngestor is a separate project and its ConfigurationBuilder currently isn't loading that same User Secrets store.

Fix this properly.

REQUIREMENTS:

1. Inspect:
   src/NovaBanking.Api/NovaBanking.Api.csproj
   tools/KnowledgeIngestor/KnowledgeIngestor.csproj
   tools/KnowledgeIngestor/Program.cs

2. Find the existing UserSecretsId used by NovaBanking.Api.

3. Configure KnowledgeIngestor to use the SAME UserSecretsId so it can access the exact same local User Secrets store.

Do not create a second copy of the secrets.

4. Add the necessary Microsoft.Extensions.Configuration.UserSecrets package to KnowledgeIngestor if required.

5. Update KnowledgeIngestor's ConfigurationBuilder so it loads:
   - appsettings.json
   - environment variables
   - the existing shared User Secrets store

The configuration hierarchy must continue supporting:

AzureAiSearch:Endpoint
AzureAiSearch:ApiKey
AzureAiSearch:IndexName
AzureAiSearch:EmbeddingDeployment
AzureAiSearch:OpenAiEndpoint
AzureAiSearch:OpenAiApiKey

6. Do NOT print any secret values to the console.

7. Do NOT change the Azure endpoints, deployment name, index name, or existing secrets.

8. Do NOT add Azure.AI.OpenAI.

9. Do NOT modify the RAG architecture or MCP implementation.

10. Run:

dotnet restore
dotnet build tools/KnowledgeIngestor
dotnet test

11. Then run:

dotnet run --project tools/KnowledgeIngestor

If configuration is now loaded, it should proceed to the PDF/Azure ingestion stage.

Do NOT ask me to paste the Azure keys into source code or appsettings.json.

Report:
- UserSecretsId used by the API
- how KnowledgeIngestor now loads the shared secrets
- package added, if any
- build result
- test result
- result of running KnowledgeIngestor

Do not expose any secret values in the report.
