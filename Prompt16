Implement OPTION 2.

Fix the KnowledgeIngestor build by removing all remaining Azure.AI.OpenAI SDK usage and using the same Azure OpenAI REST embedding approach already used by AzureAiSearchKnowledgeService.

Requirements:

1. Inspect tools/KnowledgeIngestor/Program.cs.

2. Remove:
   - using Azure.AI.OpenAI;
   - OpenAIClient
   - EmbeddingsOptions
   - any other Azure.AI.OpenAI types/usages

3. Do NOT add Azure.AI.OpenAI back to the project.

4. Implement embedding generation using HttpClient against the configured Azure OpenAI embeddings REST endpoint.

Use the existing configuration:
   - AzureAiSearch:OpenAiEndpoint
   - AzureAiSearch:OpenAiApiKey
   - AzureAiSearch:EmbeddingDeployment

The request should target the configured Azure OpenAI resource and embedding deployment using the correct Azure OpenAI REST API format for the API version supported by the existing implementation.

5. Reuse existing REST embedding logic from:
   src/NovaBanking.Infrastructure/Search/AzureAiSearchKnowledgeService.cs

Do not create a second incompatible implementation if the existing implementation can be extracted/reused cleanly.

6. The ingestion flow must remain:

PDF
→ PdfPig page extraction
→ KnowledgeIngestionService chunking
→ Azure OpenAI REST embeddings
→ Azure AI Search REST upsert

7. Do NOT replace Azure AI Search with local search.

8. Do NOT change the existing MCP implementation.

9. Do NOT change authentication, BankingService, or customer authorization.

10. Do NOT modify Azure configuration values.

11. Remove unused imports and dead code resulting from the SDK removal.

12. Run:

dotnet restore
dotnet build
dotnet test
dotnet build tools/KnowledgeIngestor

13. The final result must have:
   - zero build errors
   - KnowledgeIngestor successfully compiling
   - existing tests still passing

The NU1603 warnings about Azure.Search.Documents 12.0.0 and PdfPig may remain for now. Do not spend time fixing those warnings unless they cause an actual build/runtime failure.

IMPORTANT:
Do not add Azure.AI.OpenAI back just to make the compiler happy. The intended implementation is REST-based embeddings, consistent with AzureAiSearchKnowledgeService.

Report:
- exact changes made
- whether Azure.AI.OpenAI is completely absent from KnowledgeIngestor
- build result
- test result
- KnowledgeIngestor build result
- any remaining warnings/errors
